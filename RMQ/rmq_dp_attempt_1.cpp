#include <bits/stdc++.h>
#define maxn 1001

using namespace std;

int n, m;
int a[maxn];
int dp[maxn];

int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> n >> m;
  for (int i = 1; i <= n; i++)
    cin >> a[i];

  return 0;
}