#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

const int MAXN = 5e3 + 1;

int t, n;
ll x, a[MAXN];

int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  freopen("VAO.INP", "r", stdin);
  cin >> t;
  while (t--)
  {
    cin >> n >> x;
    ll ans = 0;
    for (int i = 1; i <= n; i++)
    {
      cin >> tmp;
        }
    cout << ans << '\n';
  }
}