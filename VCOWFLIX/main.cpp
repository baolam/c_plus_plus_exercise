#include <bits/stdc++.h>
#define maxn 17

using namespace std;
typedef long long ll;

ll c, n;
ll a[maxn];

void solve()
{

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    cin >> c >> n;
    for (ll i = 1; i <= n; i++)
        cin >> a[i];
    sort(a+1, a+1+n);

    return 0;
}
