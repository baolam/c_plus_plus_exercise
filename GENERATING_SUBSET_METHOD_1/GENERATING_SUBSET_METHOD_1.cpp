#include <bits/stdc++.h>
#define maxn 50

using namespace std;
typedef long long ll;

ll n;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    cin >> n;
    for (int i = 0; i < (1 << n); i++)
    {
        // Xử lí subset
    }
    return 0;
}
